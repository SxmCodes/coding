console.log(a);
let a = 10; // Error, because it's inside the script space, when a=undefined, it's was unable to access, but after the value, it's accessible.
var b = 100; // Undefined, because it's in global space.