if (1 == "1") {
    // JavaScript changes the datatype and checks for equallity.
    console.log('I am true.');
}
// Concept of =.
// = is used for assigning things like a = 90;
// == is used to change the datatype and checks for equallity.
// === confirms that the both values datatypes are equal or not.